// Values match the backend's `contentType` wire format (quote-editor-content-controller).
export enum QuoteSettingItemType {
  TEXT_TEMPLATE = 'TEXT_TEMPLATE',
  EXTERNAL_LINK = 'EXTERNAL_LINK',
  POLICY_DOCUMENT = 'POLICY_DOCUMENT',
}
